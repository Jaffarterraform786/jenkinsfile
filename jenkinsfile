pipeline{
  agent any
stages{
  stage('terraform init'){
    steps{
      sh "terraform init"
    }
  }
  }
  }
def getTerraformpath(){
def tfHome = name: 'terraform', type: 'terraform'
return tfHome
}

