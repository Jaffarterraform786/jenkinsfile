pipeline{
  agent any
stages{
  stage('terraform init'){
    steps{
      sh 'terraform init'
    }
  }
  }
  }
  def getTerraformPath(){
  tfHome = tool name: 'terraform-0.15', type: 'terraform'
  return tfHome
  }
